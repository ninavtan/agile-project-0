const DEFAULT_STATE = {
  _id: "61edc0a6aedb0b9422cf6ddd",
  username: "Jango",
};

export default function userReducer(state = DEFAULT_STATE, action) {
  switch(action.type) {
    default:
      return state;
  }
};