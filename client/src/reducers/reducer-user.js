const DEFAULT_STATE = {
  _id: "61f07ffb92e6bb4bf1a7d267",
  username: "Jango",
};

export default function userReducer(state = DEFAULT_STATE, action) {
  switch(action.type) {
    default:
      return state;
  }
};